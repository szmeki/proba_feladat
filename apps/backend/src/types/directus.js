export const defineDisplay = (x) => x,
  defineEndpoint = (x) => x,
  defineHook = (x) => x,
  defineInterface = (x) => x,
  defineLayout = (x) => x,
  defineModule = (x) => x,
  defineOperationApi = (x) => x,
  defineOperationApp = (x) => x,
  definePanel = (x) => x
